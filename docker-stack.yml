version: "3"
services:
  web:
    image: .
    build: .
    deploy:
      replicas: 1
    ports:
    - "8080:8080"
    depends_on:
    - mongo
  mongo:
    image: mongo
    deploy:
      replicas: 1
    ports:
    - "27017:27017"
